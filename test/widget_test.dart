import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder so `flutter test` does not fail because of the old counter
  // example. The real widget and unit tests live alongside the code under test.
  test('test suite loads', () {
    expect(true, true);
  });
}
